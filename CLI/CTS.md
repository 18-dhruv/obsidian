## the rulebook of every type the CLR understand
Every Dot net language's compiler maps its own type keyword onto cts when generating cil. this is enforced on all your code , always it makes cil from any language mutually compatible

